To use:
	-Add users
	-If needed, re-generate passkeys
	-Run server
	-If the state of the server should be saved, send SIGHUP to server and save the file titled "savedState"
	-If the server needs to stop, use Ctrl-C or send SIGINT to the server


Server arguments:
	-f [FILE]:
		Import initial server state from [FILE]
	-p [PORT]:
		Use [PORT] as server port, default is 80
